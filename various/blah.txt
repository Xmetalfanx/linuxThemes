echo ${array##*\.}

    echo "Array items:"
    for item in ${array[*]}
    do
        printf "   %s\n" $item
    done


    ELEMENT=${#array[@]}

    for (( i=0;i<ELEMENT;i++0)); do
      echo "Hello There"
      echo ${array[${i}]}
    done

read -p "Debugging pause to be removed later "
